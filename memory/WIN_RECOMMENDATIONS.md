# CALIBER — WIN RECOMMENDATIONS (increase place odds)

> Assume ~2,000 entries (doctrine). Nothing is guaranteed. Residual risk remains.

## What peers will ship (ignore / beat)

- Invalid or stub YAML
- String-match “graders”
- Homepage deepfake / weather wraps
- Marketing dashboards with fake metrics (our UI currently risks this)
- H1-only with no H2 demand story
- No X posts

## What raises win probability

1. **Dual track** — Miner **and** Script (portal allows both). Hits both judging tables.
2. **Schema-perfect YAML** + on-chain register + hash match (deletion/drift test in README).
3. **Real x402 receipt** screenshot/tx in submit package.
4. **GRADELOCK with math** — Brier (+ calibration bins) + named adversarial miners with rank deltas; not “cannot be gamed.”
5. **Labeled holdout** with provenance; agreement metric defined in README.
6. **Non-clone vertical** — default lean: AI **text** authenticity (see VERTICAL_CANDIDATES).
7. **Season slide** — H2 app that *only* works via this miner (even if Track 3 closed now).
8. **X thread** with build artifacts (explicit criterion).
9. **integrate.telegraphprotocol.com** registration + Discord early access if not done.
10. **Strip fake UI numbers** until real — judges/Discord will notice cosplay.

## Bible corrections (recommend)

| Bible / UI | Change to |
|---|---|
| Sep 7 12:00 UTC | **23:59 UTC** (live HTML 2026-08-29) |
| Vertical “in build” with fake gold set | Explicit **open** until lock + real data |
| Absolute anti-game slogans | “Resistance to gaming” + tests |
| Invented YAML fields | Official schema fields only |

## Architecture win shape (H1 production)

```
TRUTHPORT API (HTTPS)
  → YAML v1 hosted (hash)
  → registerMiner(Base Sepolia Diamond)
  → x402 paid fetch demo
GRADELOCK.wasm
  → scores miner outputs vs holdout
  → adversarial suite in CI
Marketing site
  → honest status: live vs planned
  → links to artifacts / X / explorer
```

## Security (honest enterprise bar)

- Secrets only in env; never in YAML/git/chat
- Input validation on miner API; rate limits; auth to upstream via env
- x402 facilitator trust = residual risk (document)
- WASM: deterministic, no network if host forbids; fuzz adversarial cases
- Threat model MD in repo before submit
- **Do not claim unhackable**

## API keys needed next (names)

| Key | Why |
|---|---|
| `TAVILY_API_KEY` / `TINYFISH_API_KEY` | Vertical dataset + competitor collision research (**rotate** after chat paste) |
| Base Sepolia funded wallet | `registerMiner` + x402 test pays |
| Upstream model/API key | Whatever backs TRUTHPORT (OpenAI/Venice/Google only if needed) |
| Hosting credentials | Stable HTTPS for YAML + miner |

## Build order (no half-done)

1. Lock vertical + dump labels  
2. Miner API + tests  
3. Valid YAML + host + register  
4. x402 e2e artifact  
5. GRADELOCK WASM + CI adversarial  
6. Honest UI + docs + X + submit  

Stop if any step is stub-only.

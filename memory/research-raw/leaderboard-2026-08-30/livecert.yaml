version: "1"
kind: miner
id: 4433
slug: livecert
protocol: generic
name: LiveCert Operational Signals
description: >
  Deterministic operational signals. /ssl-check performs a live TLS handshake against a domain and reports the certificate that host is actually serving right now: valid, expired, self-signed, untrusted, or issued for a different hostname, with issuer, validity window and days remaining. Unlike certificate-transparency lookups, which report what was issued, this reports what is deployed — the two disagree whenever a host still serves an old certificate. /storm-alert reports severe-weather disruption risk for a named location over the next 48 hours, graded on Beaufort gust thresholds and thunderstorm forecasts, for logistics and risk agents that need to know about storms rather than current conditions. /weather-forecast reports future conditions for a location over a stated window: the expected condition in plain words, temperature range, total precipitation and peak wind. All three answers are deterministic and independently checkable.

base_url: https://miner-wine.vercel.app

docs:
  repository: https://github.com/Harshyadav442277/miner

auth:
  type: none

rate_limit_per_sec: 20
cache_ttl_sec: 60
circuit_threshold: 5
circuit_cooldown_seconds: 30

endpoints:
  - path: /ssl-check
    external_path: /ssl-check
    method: GET
    description: >
      Live TLS certificate check for one domain. Accepts a bare hostname, a full URL, or host:port.
    intents: [SSL_VERIFICATION]
    params:
      query:
        required:
            - name: domain
              type: string
              intents:
                      - "*"
              description: >
                        Hostname to check. Accepts example.com, https://example.com/path, or example.com:8443.
        optional:
            - name: query
              type: string
              intents:
                      - "*"
              description: The user's original question, verbatim. The hostname is extracted from it.
  - path: /storm-alert
    external_path: /storm-alert
    method: GET
    description: >
      Severe-weather risk for a location over the next 48 hours. Accepts a place name or "lat,lon".
    intents: [STORM_ALERT]
    params:
      query:
        optional:
            - name: location
              type: string
              intents:
                      - "*"
              description: >
                        Place name, e.g. Chennai or Tokyo, Japan. Provide either location, or lat and lon together.
            - name: lat
              type: number
              intents:
                      - "*"
              description: >
                        Latitude in decimal degrees, -90 to 90. Use with lon when the question names coordinates.
            - name: lon
              type: number
              intents:
                      - "*"
              description: Longitude in decimal degrees, -180 to 180.
            - name: hours
              type: integer
              intents:
                      - "*"
              description: Forecast window in hours, 1 to 168. Defaults to 48.
            - name: query
              type: string
              intents:
                      - "*"
              description: "The user's original question, verbatim. Carries wording the structured fields cannot: a point in time such as 'in 44 hours', a wind threshold such as 'above 25 knots', or an operational context."
  - path: /papers
    external_path: /papers
    method: GET
    description: >
      Finds peer-reviewed papers on a topic, optionally within a date range, via OpenAlex. Returns title, authors, year and citation count for each.
    intents: [ACADEMIC_SEARCH]
    params:
      query:
        required:
            - name: topic
              type: string
              intents:
                      - "*"
              description: >
                        Research subject to search for, e.g. zero knowledge proofs. A bare topic with no question scaffolding is fine.
        optional:
            - name: query
              type: string
              intents:
                      - "*"
              description: "The user's original question, verbatim. Carries constraints the topic alone cannot: a date range, a result count, or a requested ordering."
  - path: /translate
    external_path: /translate
    method: GET
    description: >
      Translates quoted text into a named target language. Returns the translation itself, in the target script.
    intents: [LANGUAGE_TRANSLATION]
    params:
      query:
        required:
            - name: text
              type: string
              intents:
                      - "*"
              description: The text to translate, exactly as given, without surrounding quotes.
            - name: target_language
              type: string
              intents:
                      - "*"
              description: The language to translate into, by name or ISO 639-1 code, e.g. Spanish or fr.
        optional:
            - name: source_language
              type: string
              intents:
                      - "*"
              description: >
                        The language of the source text, when stated. Detected automatically when omitted.
            - name: query
              type: string
              intents:
                      - "*"
              description: >
                        The user's original question, verbatim, e.g. Translate "Good morning" into French.
  - path: /ip-geolocate
    external_path: /ip-geolocate
    method: GET
    description: >
      Live IP geolocation. Accepts an IPv4 or IPv6 address, or a whole question containing one.
    intents: [IP_GEOLOCATION]
    params:
      query:
        required:
            - name: ip
              type: string
              intents:
                      - "*"
              description: IPv4 or IPv6 address to geolocate, e.g. 8.8.8.8.
        optional:
            - name: query
              type: string
              intents:
                      - "*"
              description: The user's original question, verbatim. The address is extracted from it.
  - path: /weather-forecast
    external_path: /weather-forecast
    method: GET
    description: >
      Future conditions for a location over a stated window (default 24h, max 168h). Accepts a place name or "lat,lon", plus an optional hours parameter.
    intents: [WEATHER_FORECAST]
    params:
      query:
        optional:
            - name: location
              type: string
              intents:
                      - "*"
              description: >
                        Place name, e.g. New York City. Provide either location, or lat and lon together.
            - name: lat
              type: number
              intents:
                      - "*"
              description: >
                        Latitude in decimal degrees, -90 to 90. Use with lon when the question names coordinates.
            - name: lon
              type: number
              intents:
                      - "*"
              description: Longitude in decimal degrees, -180 to 180.
            - name: days
              type: integer
              intents:
                      - "*"
              description: Forecast span in days, 1 to 16, e.g. 7 for a 7-day forecast.
            - name: hours
              type: integer
              intents:
                      - "*"
              description: Forecast window in hours, 1 to 168. An alternative to days.
            - name: query
              type: string
              intents:
                      - "*"
              description: >
                        The user's original question, verbatim. Carries wording the structured fields cannot, such as an explicit start date like 'starting September 1st' — without it the forecast starts today.
  - path: /ai-detect
    external_path: /ai-detect
    method: GET
    description: >
      Statistical authorship analysis of a supplied passage. Measures sentence-length burstiness, type-token ratio, repeated word pairs and formulaic connectives, and reports what they indicate with a calibrated confidence that is capped at 0.6. Authorship detection is not reliable enough to treat as proof and the answer says so; when no passage long enough to analyse is supplied it says that instead of guessing.
    intents: [AI_TEXT_DETECTION]
    params:
      query:
        required:
            - name: text
              type: string
              intents:
                      - "*"
              description: >
                        The passage to analyse for authorship. At least about 40 words of continuous prose; below that no statistic is meaningful.
        optional:
            - name: query
              type: string
              intents:
                      - "*"
              description: >
                        The user's original question, verbatim, e.g. Is the following text written by an AI or a human?

input_schema:
  type: object
  properties:
    query:
        type: string
        description: "The user's original question, verbatim. Preferred over the structured fields: it carries detail they cannot, such as \"starting next Monday\", \"in 44 hours\", or \"flag winds above 25 knots\"."
    q:
        type: string
        description: Alias for query.
    domain:
        type: string
        description: >
                /ssl-check. Hostname to check. Accepts "example.com", "https://example.com/path", or "example.com:8443".
    location:
        type: string
        description: >
                /storm-alert and /weather-forecast. Place name, e.g. "Chennai" or "Tokyo, Japan".
    latitude:
        type: number
        description: >
                /storm-alert and /weather-forecast. Use with longitude when the question names coordinates rather than a place.
        minimum: -90
        maximum: 90
    longitude:
        type: number
        minimum: -180
        maximum: 180
    lat:
        type: number
        description: Alias for latitude.
    lon:
        type: number
        description: Alias for longitude.
    hours:
        type: integer
        description: >
                /storm-alert and /weather-forecast. Forecast window in hours. Also read from the question itself, e.g. "over the next 44 hours".
        minimum: 1
        maximum: 384
    forecast_hours:
        type: integer
        description: Alias for hours.
    days:
        type: integer
        description: Forecast span in days, converted to hours.
        minimum: 1
        maximum: 16
    forecast_days:
        type: integer
        description: Alias for days.
    topic:
        type: string
        description: /papers. Research subject to search for, e.g. "zero knowledge proofs".
    ip:
        type: string
        description: /ip-geolocate. IPv4 or IPv6 address.
    text:
        type: string
        description: >
                /translate and /ai-detect. The text to translate, or the passage whose authorship is to be analysed, with or without quotes. Registration 260 declared no translation-shaped parameter and both scored epochs arrived with none filled — the engine fills only parameters a miner declares, so this field is what makes translation questions reach us.
    target_language:
        type: string
        description: /translate. The language to translate into, by name or ISO 639-1 code.
    source_language:
        type: string
        description: /translate. The language of the source text, when stated.

output_schema:
  type: object
  properties:
    domain:
        type: string
    chain_length:
        type:
              - integer
              - "null"
        description: Certificates the server presented, leaf included. /ssl-check only.
    chain_complete:
        type:
              - boolean
              - "null"
        description: Whether intermediates were sent, not just the leaf.
    subject_alt_names:
        type:
              - array
              - "null"
        description: Subject Alternative Names on the leaf certificate.
    unreachable_reason:
        type:
              - string
              - "null"
        description: Why the host could not be reached, when it could not be.
    start_time:
        type:
              - string
              - "null"
        description: >
                First hour of the returned series, UTC. /weather-forecast honours an explicit start named in the question.
    end_time:
        type:
              - string
              - "null"
    hourly_count:
        type: integer
        description: Number of hourly values returned.
    hourly_min_precipitation_mm:
        type:
              - number
              - "null"
    hourly_max_precipitation_mm:
        type:
              - number
              - "null"
    time_mode:
        type: string
        description: >
                "point" when the question asked about one moment ("in 44 hours", "right now"); "window" when it asked about a span ("over the next 44 hours").
        enum:
              - point
              - window
    valid_at:
        type:
              - string
              - "null"
        description: For a point answer, the UTC hour the values describe. Null for a window.
    risk_score:
        type: number
        description: Storm risk from 0 (none) to 1 (severe). /storm-alert only.
        minimum: 0
        maximum: 1
    verdict:
        type: string
        description: >
                Canonical status. /ssl-check returns a certificate state; /storm-alert returns a storm-risk grade.
        enum:
              - valid
              - expired
              - not_yet_valid
              - hostname_mismatch
              - self_signed
              - untrusted
              - unreachable
              - none
              - low
              - moderate
              - high
              - severe
              - unknown
              - clear
              - mostly clear
              - cloudy
              - fog
              - drizzle
              - rain
              - heavy rain
              - snow
              - thunderstorms
    valid:
        type: boolean
    trusted:
        type: boolean
    expired:
        type: boolean
    hostname_match:
        type: boolean
    issuer:
        type:
              - string
              - "null"
    subject:
        type:
              - string
              - "null"
    valid_from:
        type:
              - string
              - "null"
    valid_to:
        type:
              - string
              - "null"
    days_remaining:
        type:
              - integer
              - "null"
    tls_protocol:
        type:
              - string
              - "null"
    confidence:
        type: number
    reason:
        type: string
    checked_at:
        type: string
    location:
        type:
              - string
              - "null"
    storm_expected:
        type: boolean
    max_wind_gust_kmh:
        type:
              - number
              - "null"
    max_wind_speed_kmh:
        type:
              - number
              - "null"
    max_precipitation_mm:
        type:
              - number
              - "null"
    thunderstorm:
        type: boolean
    window_hours:
        type:
              - integer
              - "null"
    peak_at:
        type:
              - string
              - "null"
    latitude:
        type:
              - number
              - "null"
    longitude:
        type:
              - number
              - "null"
    temp_min_c:
        type:
              - number
              - "null"
    temp_max_c:
        type:
              - number
              - "null"
    total_precipitation_mm:
        type:
              - number
              - "null"
    precipitation_hours:
        type:
              - integer
              - "null"

semantics:
  signal_mapping:
    confidence_field: confidence
    label_field: verdict
    reason_field: reason
  supported_intents:
    - SSL_VERIFICATION
    - STORM_ALERT
    - WEATHER_FORECAST
    - IP_GEOLOCATION
    - LANGUAGE_TRANSLATION
    - ACADEMIC_SEARCH
    - AI_TEXT_DETECTION
